#ifndef LOCALISATIONITEM_H
#define LOCALISATIONITEM_H
/*
#include "listmodel.h"

class LocalisationItem : public ListItem
{
    Q_OBJECT


    public:
        enum Roles {
            LongeRole = Qt::UserRole+1,
            LatRole//,
            //PriceRole
        };

    public:
        LocalisationItem(QObject *parent = 0): ListItem(parent) {}
        explicit LocalisationItem(const int &longe, const int &lat, QObject *parent = 0);
        QVariant data(int role) const;
        QHash<int, QByteArray> roleNames() const;
        //void setPrice(qreal price);
        inline int longe() const { return m_longe; }
        inline int lat() const { return m_lat; }

    private:
        int m_longe;
        int m_lat;
};

#endif // LOCALISATIONITEM_H
*/
